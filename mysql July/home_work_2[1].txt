-- Creating the first table without constraints
CREATE TABLE Table1 (
    id INT,
    name VARCHAR(50),
    age INT
);

-- Inserting some values into Table1
INSERT INTO Table1 (id, name, age) VALUES
(1, 'John', 30),
(2, 'Alice', 25),
(3, 'Bob', 35);

-- Creating the second table without constraints
CREATE TABLE Table2 (
    product_id INT,
    product_name VARCHAR(50),
    price DECIMAL(10, 2)
);

-- Inserting some values into Table2
INSERT INTO Table2 (product_id, product_name, price) VALUES
(101, 'Laptop', 1200.00),
(102, 'Smartphone', 800.00),
(103, 'Tablet', 500.00);

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


1.Add a primary key constraint on the id column.

->Alter table table1 add primary key(id);

2.Add a check constraint to ensure age is a positive integer.

->Alter table table1 add constraint age_psinteger check age>0;

3.Add a primary key constraint on the product_id column.
->Alter table table2 add primary key product_id;

4.Add a check constraint to ensure price is non-negative.
->Alter table table2 add constraint price_nonneg check (price>0);

5.Add a unique constraint on the name column.
->alter table table1 add constraint unique(name);

6.Adding a unique constraint on the combination of product_id and product_name.
->alter table table2 add constraint unique(product_id,product_name);

7.Rename a Column of  table1 ,Rename a Column of table2 
->alter table table1 rename column id to customer_id;
alter table table2 rename column price to product_price;
8.Rename a Table1,Rename a table2
->alter table table1 rename to employee_details;
alter table table1 rename to product_details;
9.Drop a Column  table1 ,Drop a Column  table2
->alter table employee_details drop name;
  alter table product_details drop product_name;
10.Add a Column table1 , Add a Column  table2
->alter table table_name add column column_name datatype;
alter table table_name add column column_name datatype;
11.Modify a Column (Change Data Type) table1 ,Modify a Column (Change Data Type) table2.
->alter table table1 modify column name new datatype;


12.Drop a Constraint table1 ,Drop a Constraint table2.
->alter table table_name drop constraint constraint_name;



13.create any 2 tables of your choice  with constraints.
means while creating the table simply give the constraints

CREATE TABLE Employees (
    employee_id INT PRIMARY KEY,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    department VARCHAR(50),
    salary DECIMAL(10, 2) DEFAULT 0.00,
    CONSTRAINT check_salary CHECK (salary >= 0),
    CONSTRAINT unique_name UNIQUE (first_name, last_name)
);

CREATE TABLE Orders (
    order_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_id INT NOT NULL,
    order_date DATE NOT NULL,
    total_amount DECIMAL(10, 2) NOT NULL,
    CONSTRAINT fk_customer_id FOREIGN KEY (customer_id) REFERENCES Customers(customer_id),
    CONSTRAINT check_total_amount CHECK (total_amount >= 0)
);
